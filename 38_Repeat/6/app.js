// Написать функцию на подсчет количества пар ключ значение в объекте. Добавить
// проверки

let object = {
  isValid: true,
  id: 123,
  lg: `english`,
};

function check(obj){
  try {
    let arr
  } catch (error) {
    
  }
}